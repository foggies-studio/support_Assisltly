{
  "name": "support-bot",
  "version": "1.0.0",
  "private": true,
  "description": "MVP Telegram support bot for Support Assistly",
  "main": "dist/index.js",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "dotenv": "^16.6.1",
    "telegraf": "^4.16.3"
  },
  "devDependencies": {
    "@types/node": "^20.17.0",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.6.3"
  }
}
